const quarterValue = (numberToDivide) => {

    let numberDivided = numberToDivide / 4;
    return numberDivided;

}

// Invoke the function and store the return value
const finalNumber = quarterValue(40);

// Log the return value
console.log(finalNumber)